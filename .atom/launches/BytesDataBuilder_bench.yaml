# Cli launch configuration for benchmark\BytesDataBuilder_bench.dart.
type: cli
path: benchmark\BytesDataBuilder_bench.dart

cli:
  # Additional args for the application.
  args:
  # The working directory to use for the launch.
  cwd:
  # Enable or disable checked mode.
  checked: false
  # Enable or disable debugging.
  debug: true
